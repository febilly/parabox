from time import time

logging = False

file_list = ["Button.png", "PlayerButton.png", "FastTravel.png", "Info.png",
             "Infinity.png", "Player.png", "PlayerButton.png", "Possessable.png"]


def blit(target_graphic, x, y, source_graphic):
    # dummy function to stop IDEs from complaining
    if logging:
        print("blit", target_graphic, x, y, source_graphic)
    pass


def strblit2(target_graphic, x, y, width, height, source_graphic, source_x, source_y, source_width, source_height):
    # dummy function to stop IDEs from complaining
    if logging:
        print("strblit2", target_graphic, x, y, width, height, source_graphic, source_x, source_y, source_width, source_height)
    pass


def dimgrob(graphic, width, height, color):
    # dummy function to stop IDEs from complaining
    if logging:
        print("dimgrob", graphic, width, height, color)
    pass


def rect(graphic, x, y, width, height, color):
    # dummy function to stop IDEs from complaining
    if logging:
        print("rect", graphic, x, y, width, height, color)
    pass


def fillrect(graphic, x, y, width, height, color_edge, color_fill):
    # dummy function to stop IDEs from complaining
    if logging:
        print("fillrect", graphic, x, y, width, height, color_edge, color_fill)
    pass


def grobh(graphic):
    # dummy function to stop IDEs from complaining
    if logging:
        print("grobh", graphic)
    pass


def grobw(graphic):
    # dummy function to stop IDEs from complaining
    if logging:
        print("grobw", graphic)
    pass


def eval(string):
    # dummy function to stop IDEs from complaining
    if logging:
        print("eval", string)
    if string == "time":
        return time()
    elif string == "AFiles()":
        return file_list
    elif string == "GETKEY":
        return -1
    pass


def textout(graphic, x, y, text, color):
    # dummy function to stop IDEs from complaining
    if logging:
        print("textout", graphic, x, y, text, color)
    pass
